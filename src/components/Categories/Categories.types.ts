// Interfaces and types from component Categories

// Component Props
export interface CategoriesProps {}

export interface Category {
  id: string;
  name: string;
  description: string;
  icon: React.ReactNode;
}
